import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Social Strategy — Joyn 2026 Playbook',
  description: 'Interactive social media strategy for Joyn: content pillars, brand voice, competitor analysis, and engagement projections.',
}

export default function StrategyLayout({ children }: { children: React.ReactNode }) {
  return children
}
